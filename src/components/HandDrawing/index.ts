// Export the hand drawing component
import HandDrawing from './HandDrawing';
export default HandDrawing;

// Re-export the hook from context for convenience
export { useHandGesture } from '../../context/HandGestureContext'; 